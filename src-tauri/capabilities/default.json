{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The chat window loads the local server; it needs no Tauri APIs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
